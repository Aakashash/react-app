import React from "react";
import { ReactDOM } from "react";

function header() {
  return (
    <header>
      <nav>
        <h1>React </h1>
      </nav>
    </header>
  );
}

export default header;
